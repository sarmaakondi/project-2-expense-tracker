<nav>
  <% if (user) { %>
  <a href="/users/<%= user._id %>/transactions">Your Transactions</a>
  <button id="goback-btn" onclick="window.history.back()">Go Back</button>
  <a href="/auth/sign-out">Sign Out</a>
  <% } else { %>
  <a href="/auth/sign-up">Sign Up</a>
  <a href="/auth/sign-in">Sign In</a>
  <% } %>
</nav>
